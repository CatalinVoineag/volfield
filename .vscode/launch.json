{
  "version": "0.2.0",
  "configurations": [{
    "name": "Debug Volfield",
    "type": "cppdbg",
    "request": "launch",
    "program": "${workspaceFolder}/build-debug/volfield",
    "cwd": "${workspaceFolder}",
    "preLaunchTask": "build"
  }]
}
